function [imageConv] = convolution(image, mask)
    imageConv = image;
    sumMask = sum(mask, "all");
    if sumMask ~= 0
        mask = mask / sumMask;
    end
    [iImage, jImage, kImage] = size(image);
    skip = floorDiv(size(mask, 1), 2);
    for k=1:kImage % rgb loop
        for j=1+skip:jImage-skip
            for i=1:iImage
                
            end
        end
    end
end